import 'package:flutter/material.dart';


/// Collection des Nutzers
class CollectionScreen extends StatelessWidget {
  const CollectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
    );
  }
}
